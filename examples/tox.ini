[tox]
envlist =
    py{36,37,38,39}

[testenv]
description = Unit tests
deps =
    cli-test-helpers
    py39: setuptools>=50
    pytest
commands =
    pytest {posargs}

[pytest]
addopts =
    --color=yes
    --doctest-modules
    --verbose
